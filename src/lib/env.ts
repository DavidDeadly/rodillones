export const {
	DB,
	DB_USER,
	DB_PASS,

	PUSHER_SECRET,
	PUSHER_APP_ID,
	NEXT_PUBLIC_PUSHER_KEY,
	NEXT_PUBLIC_PUSHER_CLUSTER,

	WHAPI_TOKEN,
	ZAPLY_INSTANCE,
	ZAPLY_TOKEN,

	NEXT_PUBLIC_SUPABASE_URL,
	NEXT_PUBLIC_SUPABASE_KEY,
} = process.env;
